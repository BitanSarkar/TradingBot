growwapi>=1.5.0
pyotp>=2.9.0
python-dotenv>=1.0.0

# NSE data sources
nselib>=2.0.0
nsepy>=0.8
# pandas_market_calendars is a transitive dep of nselib.
# v4.4+ uses `X | Y` union syntax in function signatures which requires
# Python 3.10+.  Pin to <4.4 so the bot runs on Python 3.9 (EC2 Amazon Linux).
pandas_market_calendars>=3.6,<4.4

# Data processing
pandas>=2.0.0,<3.0.0   # pandas 3.x drops some APIs used by nselib/nsepy
numpy>=1.26.0
scipy>=1.12.0               # norm.ppf for parametric VaR in exit_signals.py
requests>=2.31.0
urllib3>=2.0.0          # used directly for Retry + HTTPAdapter pool tuning
pyarrow>=15.0.0         # pandas parquet backend (data/cache.py)
yfinance>=0.2.0         # OHLCV fallback #2 — Yahoo Finance NSE mirror (stderr-suppressed)
jugaad-data>=0.26       # OHLCV fallback #4 — jugaad_data.nse.stock_df (NSE historical)
boto3>=1.34.0           # AWS SNS for end-of-day email notifications

# News & sentiment
feedparser>=6.0.0
vaderSentiment>=3.3.2

# FinBERT — financial-domain BERT sentiment model (~400 MB, one-time download)
# Pinned tightly to avoid pip resolver backtracking on transitive CUDA deps.
# On EC2 (no GPU) install CPU-only torch manually:
#   pip install torch==2.6.0+cpu --index-url https://download.pytorch.org/whl/cpu --no-cache-dir
transformers>=4.40.0,<4.48.0
torch>=2.6.0,<2.7.0
